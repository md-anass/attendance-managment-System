{
  "scripts": {
    "dev": "npm run dev --prefix attendance-management",
    "build": "npm run build --prefix attendance-management",
    "start": "npm run start --prefix attendance-management",
    "lint": "npm run lint --prefix attendance-management"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.110.2",
    "clsx": "^2.1.1",
    "lucide-react": "^1.24.0",
    "recharts": "^3.9.2",
    "tailwind-merge": "^3.6.0"
  }
}
